#include <stdlib.h>
#include <ctype.h>
#include "main.h"
/**
 * _abs - prints absolute number
 * @c: character to be checked
 * Return: should return the result of num
 */
int _abs(int c)
{
int num;
num = abs(c);
return num;
return (0);
}
