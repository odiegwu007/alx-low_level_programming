#include <stdlib.h>
#include <ctype.h>
#include "main.h"
/**
 * add - performs the addition of two ints
 * @a: first character to be added
 * @b: second character to be added
 * Return: should return the result of sum
 */
int add(int a, int b)
{
int sum;
sum = a + b;
return (sum);
}
