#include "main.h"
/**
 * _islower - prints 1 if character is lowercase
 * Return: should return 1 if lowercase
 */
int _islower(int c)
{
if (islower(c))
{
return (1);
}
else
{
return (0);
}
return (0);
}
